{
  "name": "n8n-audio-merge",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers API to merge audio segments via ffmpeg.wasm for n8n",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240808.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.0.0"
  }
}
